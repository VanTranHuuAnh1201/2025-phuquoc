# Firecrawl — lấy key miễn phí tại https://www.firecrawl.dev/app/api-keys
FIRECRAWL_API_KEY=fc-xxxxxxxxxxxxxxxxxxxx

# Chỉ set khi self-host Firecrawl bằng docker compose (repo firecrawl/firecrawl).
# Khi đã set, FIRECRAWL_API_KEY có thể bỏ trống.
# FIRECRAWL_API_URL=http://localhost:3002
