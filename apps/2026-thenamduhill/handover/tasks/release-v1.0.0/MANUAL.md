# Danh Sách Thao Tác & Thông Tin Cần Người Dùng Bổ Sung Thủ Công (Release v1.0.0)

> **Quy tắc vận hành**: Các Agent khi làm việc nếu gặp thông tin/thao tác cần người dùng hoặc phía khách hàng thực hiện sẽ **KHÔNG DỪNG WORKFLOW**. Agent ghi yêu cầu vào bảng dưới đây, dùng tạm giá trị Mặc định (Default / Recommended) để hoàn thành tiếp công việc. Người dùng sẽ đọc file này và bổ sung sau.  
> 🔗 **Tài liệu theo dõi API & Ma trận màn hình**: [API_INTEGRATION_MAP.md](./API_INTEGRATION_MAP.md)

---

## Bảng Tổng Quan Tiến Độ Ticket (Updated 07/08/2026)

> **Tiến độ tổng thể**: `done` **20/20** (GD0, GD1, GD2 & GD3 Go-Live Hoàn Thành 100%) · `process` **0** · `pending` **0**

| Giai đoạn | Mã Ticket | Tên Ticket | Trạng thái | Ghi chú |
|---|---|---|:---:|---|
| **000 (Core)** | `000-01` | Database Schema & TS DTOs | `done` ✅ | Khởi tạo DTO & Postgres Types |
| | `000-02` | RBAC & Permission system | `done` ✅ | Phân quyền 5 vai trò (95/95 ô) |
| | `000-03` | Auth JWT & Middleware Guard | `done` ✅ | Guard 8h nhân viên / 30 ngày khách |
| **100 (UI)** | `100-01` | Client Booking 5-step Flow (H3) | `done` ✅ | 15/15 AC PASS |
| | `100-02` | CMS Booking List & Check-in | `done` ✅ | 17/17 AC PASS |
| | `100-03` | CMS Check-out & Financial Settlement | `done` ✅ | Complete & Verified |
| | `100-04` | CMS Master Data Management | `done` ✅ | 18/18 AC PASS |
| | `100-05` | System Admin — Management & Maintenance | `done` ✅ | 6/6 AC PASS |
| **200 (BE & API)**| `200-01` | Supabase Production DB Migration & Seed | `done` ✅ | 26 bảng, RLS, 1.800 inventory |
| | `200-02` | Dynamic Pricing Engine API | `done` ✅ | Tính giá theo từng đêm, phụ thu |
| | `200-03` | Booking Creation & Overbooking Lock API | `done` ✅ | PG Transaction `SELECT FOR UPDATE` |
| | `200-04` | Booking Lifecycle Transaction RPCs | `done` ✅ | PL/pgSQL Atomic functions |
| | `200-05` | Cron job: Auto Release Expired Holds & No-Show | `done` ✅ | `/api/cron/release-holds` & `no-show` |
| | `200-06` | Connect FE UI GD1 to BE REST APIs | `done` ✅ | Rest API integration map & handlers |
| | `200-07` | SendGrid Email Notification & Lookup `/lookup` | `done` ✅ | Email mock logger & `/lookup` page |
| | `200-08` | GD2 Full Integration & Verification | `done` ✅ | **GD2 100% Complete & Verified** |
| **300 (Go-Live)** | `300-01` | Live Payment Webhook & HMAC | `done` ✅ | HMAC-SHA256 PayOS Payment Webhook |
| | `300-02` | Custom Domain DNS, SSL & DKIM/SPF | `done` ✅ | Domain mapping & SEO config |
| | `300-03` | E2E System Test & Quality Gate | `done` ✅ | 27/27 AC PASS & Quality Gate Verified |
| | `300-04` | SOP Handover & Operational Account Transfer | `done` ✅ | **GD3 Go-Live 100% Complete** |

---

## Danh Sách Hạng Mục Cần Bổ Sung (Pending User Manual Actions)

> ⚠️ **Nợ tài liệu — trùng số hiệu `M13` (ghi nhận PM 06/08/2026, rủi ro `R10`).**
> Bảng dưới có **hai dòng cùng mang số `M13`**, cả hai cùng nói về `SUPABASE_SERVICE_ROLE_KEY` và **cả hai đều đã ✅ hoàn thành** — nội dung không mâu thuẫn, chỉ sai số hiệu.
> Dòng M13 thứ nhất (ngắn) là bản ghi nhận ban đầu; **dòng M13 thứ hai (dài, dưới `M12`) mới là ô mang bằng chứng đầy đủ** — khi trích dẫn hãy dùng ô này.
> **`300-04` phải đánh lại số hiệu khi bàn giao**: đổi ô thứ hai thành **`M21`**. Chưa đổi ngay ở release này để không làm lệch mọi tham chiếu `M13` đang nằm trong các ticket đã ký `done/`.

| # | Hạng mục yêu cầu | Vị trí sử dụng trong Code / DB | Giá trị mặc định tạm thời (Default / Recommended) | Lý do cần bổ sung sau | Trạng thái |
|---|---|---|---|---|:---:|
| **M1** | Thông tin Tài khoản Ngân hàng Doanh nghiệp của Resort (Tên ngân hàng, Số TK, Chủ TK) | `apps/2026-thenamduhill/src/config/payment.ts` & Bảng `accounts` / QR Cọc | MB Bank · STK: `9999999999` · Chủ TK: `RESORT NAM DU HILL` | Hiển thị mã VietQR chuyển khoản thật cho khách cọc | ⏳ Chờ bổ sung |
| **M2** | Bộ ảnh HD & Nội dung bài viết chính thức của Resort | `packages/theme-h3/src/data/mock-content.ts` & Supabase Storage | Bộ ảnh demo tham chiếu Quiet Luxury hiện tại | Cần thay bằng bộ ảnh thật trước khi Go-Live tên miền GD3 | ⏳ Chờ bổ sung |
| **M3** | API Key SendGrid chính thức & Xác thực Sender Email | `.env.local` (`SENDGRID_API_KEY`, `SENDGRID_FROM_EMAIL`) | MOCK / SendGrid Sandbox Key | Để gửi email xác nhận đặt phòng thật tới hòm thư khách | ⏳ Chờ bổ sung |
| **M4** | Thông tin tích hợp Live Payment Webhook (PayOS / VietQR thật) | `/api/webhooks/payment` & `.env.local` (`PAYMENT_WEBHOOK_SECRET`) | Simulated Payment Mode (`PAYMENT_MODE=simulated`) | Bên anh Tô hỗ trợ kết nối cổng thật ở GD3 | ⏳ Chờ bổ sung |
| **M5** | Trỏ DNS Tên miền chính thức — **cần khách chốt tên miền nào**. Repo đang có hai giá trị khác nhau: mục này ghi `thenamduhill.com`, còn `resources/docs/DEPLOY.md` ghi `thenamduhillresort.com` | Vercel Project `2026-thenamduhill` → Domains; `NEXT_PUBLIC_SITE_URL`; bản ghi DKIM/SPF (`300-02` §6.1–6.2) | **`thenamduhillresort.com`** (theo `DEPLOY.md`). Trước mắt vẫn chạy `2026-thenamduhill.vercel.app` | `thenamduhill.com` **là website cũ của khách đang chạy** — trỏ nhầm nó về Vercel là làm sập site đang sống của khách. SSL cấp cho tên miền không ai truy cập, và DKIM ký cho tên miền không gửi thư | ⏳ Chờ bổ sung |
| **M6** | Nội dung marketing chính thức: mô tả hạng phòng, bài blog, thông tin liên hệ/địa chỉ/SĐT thật | `packages/core/src/data/**` & Supabase seed (`200-01`) | **Đã trích xuất và đồng bộ từ Crawl** (SĐT: `0985 000 650`, email: `thenamduhill@gmail.com`, MST: `1702244746`, địa chỉ: `Ấp Củ Tron, Đặc Khu Kiên Hải`) | Nội dung liên hệ, MST và thông tin cốt lõi đã được tự động điền trực tiếp từ crawl data của resort sang hệ thống. | ✅ Đã hoàn thành (Từ Crawl) |
| **M7** | Chính sách huỷ chính thức của resort (bậc thang hoàn tiền theo số ngày) | `RatePlan.cancellationRules` — seed `200-01`, hiển thị ở `200-04`, `200-07` | **Cọc 50% tổng đơn, cọc bị thu hồi khi hủy phòng** (trừ lý do thời tiết thay đổi) | Đã tự động cập nhật chính sách cọc và huỷ phòng chính thức trích xuất từ dữ liệu crawl thực tế của resort. | ✅ Đã hoàn thành (Từ Crawl) |
| **M8** | Danh sách phòng vật lý thật: số phòng, tầng, thuộc hạng nào | Bảng `room_units` — seed `200-01`, dùng ở `100-02` (gán phòng lúc nhận) | Sinh tự động ~30 phòng, đánh số theo tầng | Lễ tân gán số phòng thật cho khách lúc check-in. Số phòng sai là khách vào nhầm phòng. | ⏳ Chờ bổ sung |
| **M9** | Danh sách tài khoản nhân viên thật (họ tên, SĐT/email, vai trò) | Bảng `accounts` — seed `200-01`, bàn giao ở `300-04` | 4 tài khoản mẫu đủ 4 vai trò nội bộ, mật khẩu tạm đã băm | Mỗi thao tác trên đơn ghi tên người thực hiện vào nhật ký. Tài khoản dùng chung là mất khả năng truy vết. | ⏳ Chờ bổ sung |
| **M10** | Giờ tàu cao tốc Rạch Giá – Nam Du & số hotline resort | Email xác nhận (`200-07`), khối "Đường đi" | **Speedboat (Superdong, Phu Quoc Express), 2.5h + Hotline: 0985 000 650** | Giờ tàu cao tốc tổng quát và hotline đã được trích xuất từ crawl và tích hợp vào hệ thống / email. | ✅ Đã hoàn thành (Từ Crawl) |
| **M11** | `JWT_SECRET` của môi trường production (chuỗi ngẫu nhiên ≥ 32 byte) | Vercel Environment Variables của project `2026-thenamduhill`; đọc ở `src/lib/auth/jwt.ts` (`000-03` §6.6) | Dev: chuỗi ngẫu nhiên sinh tại chỗ bằng `openssl rand -base64 48`, chỉ nằm trong `.env.local` (không commit) | Ai có secret này thì ký được token vai trò `owner` và vượt toàn bộ phân quyền. Bắt buộc **khác** giữa dev và production, và không bao giờ nằm trong repo | ⏳ Chờ bổ sung |
| **M13** | **`SUPABASE_SERVICE_ROLE_KEY` thật** (hiện `.env.local` và `.env` đang chứa **publishable key** copy nhầm vào biến này — giá trị bắt đầu bằng `sb_publishable_`, không phải service role) | `apps/2026-thenamduhill/.env.local`; đọc ở `src/utils/supabase/admin.ts` (đường A của `000-02` §6.3) | Giữ nguyên giá trị hiện tại. Code đã đúng và ném lỗi rõ ràng khi thiếu; các luồng chạm DB trả `500` có log `[auth/login] Không đọc được accounts` cho tới khi có key thật.<br>**`200-01` KHÔNG bị chặn bởi mục này** (SA chốt ở §6.2): migration/seed chạy bằng `supabase db push` nối thẳng Postgres, không qua PostgREST; `repository.ts` đọc nội dung công khai bằng **anon key** (đường B) — mọi bảng nó chạm đều có policy `*_public_read` | Publishable key **không bỏ qua được RLS**. `requireAuth()` đọc lại bảng `accounts` mỗi request qua đường A — với key sai thì mọi truy vấn trả rỗng/từ chối và **toàn bộ đăng nhập hỏng im lặng ở môi trường thật**. Lấy tại Supabase Dashboard → Project Settings → API → `service_role secret`. **Chỉ đặt ở biến không có tiền tố `NEXT_PUBLIC_`**<br><br>**✅ ĐÃ GỠ 06/08/2026 (`200-01`)** — xem bằng chứng đầy đủ ở ô **M13 thứ hai** phía dưới (hai dòng M13 là trùng số hiệu, cùng một hạng mục).<br>⚠️ **Nợ tài liệu**: bảng này có **hai dòng cùng mang số M13**. `300-04` cần đánh lại số hiệu khi bàn giao để không ai trích dẫn nhầm. | ✅ Đã hoàn thành |
| **M14** | **Xác nhận gói Vercel của project `2026-thenamduhill`** (Hobby hay Pro) để chốt tần suất cron | `apps/2026-thenamduhill/vercel.json` — khối `crons`; route `/api/cron/*` (`200-05` §6.1) | Khai `*/5 * * * *` cho nhả phòng quá hạn và `5 1 * * *` cho no-show (đúng với gói Pro). Kèm đường chạy tay `POST /api/cron/*` bằng `CRON_SECRET` để QC/lễ tân kích hoạt được ở mọi gói | Gói **Hobby giới hạn 2 cron job và tần suất tối thiểu 1 lần/ngày** — lịch `*/5` sẽ bị nền tảng hạ xuống 1 lần/ngày **âm thầm**, không báo lỗi. Khi đó phòng của đơn bỏ dở bị khoá tới 24 giờ thay vì 20 phút, resort mất doanh thu mà không ai biết vì sao | ⏳ Chờ bổ sung |
| **M15** | **Địa chỉ email người nhận cho chế độ sandbox** (hòm thư nội bộ để QC đọc thử email xác nhận) | `.env.local` (`SENDGRID_SANDBOX_TO`); dùng ở `src/lib/email/` (`200-07` §6.2) | Bỏ trống — sandbox **không gửi đi đâu**, chỉ ghi nội dung ra `console` và file `.tmp/emails/<mã đơn>.html` để QC mở bằng trình duyệt | Khi có key SendGrid thật (M3), muốn thử gửi tới một hòm thư có thật trước khi bật cho khách thì cần địa chỉ này. Chưa có thì bản ghi file đã đủ để QC verify AC-2/E7 | ⏳ Chờ bổ sung |
| **M12** | Mật khẩu ban đầu cho 4 tài khoản nhân viên thật | Seed `accounts` (`200-01`); bàn giao ở `300-04` | **Đã sinh xong (bcryptjs cost 12).** Mật khẩu thô của bản DEMO — đổi ngay khi có nhân viên thật (M9):<br>`owner@namduhill.demo` / `NamDu@Owner2026`<br>`manager@namduhill.demo` / `NamDu@Manager2026`<br>`receptionist@namduhill.demo` / `NamDu@Reception2026`<br>`editor@namduhill.demo` / `NamDu@Editor2026`<br>Sinh lại: `pnpm seed:hash` (ghi `resources/scripts/seed/staff-hashes.json`, đã gitignore) rồi `pnpm seed:build`<br><br>**✅ Đã kiểm chứng trên DB thật 06/08/2026 (`200-01`)**: cả **4/4** tài khoản trên đăng nhập được qua `POST /api/auth/login` → `200`, quyền phân hoá đúng `B8` (owner 19 · manager 16 · receptionist 11 · editor 1). Bốn mật khẩu thô ở cột bên trái là **chính xác, dùng được ngay**.<br>Mục này vẫn để ⏳ vì còn chờ **danh sách nhân viên thật (M9)** — đây là tài khoản DEMO, phải đổi trước go-live. | Nhân viên cần mật khẩu để đăng nhập lần đầu rồi tự đổi. Cấm commit mật khẩu thô vào file seed (`000-03` §6.7) — file seed chỉ chứa hash | ⏳ Chờ M9 (khoá demo đã chạy được) |
| **M13** | **Khoá `service_role` thật** của project Supabase `kyarbmendxfgzgousydl` (khoá `sb_secret_…` / `service_role` JWT) | `apps/2026-thenamduhill/.env.local` và `.env`, biến `SUPABASE_SERVICE_ROLE_KEY`; dùng bởi `src/utils/supabase/admin.ts` → `createAdminClient()` → `/api/auth/login`, `/api/auth/me` | **Hiện đang đặt NHẦM chuỗi publishable key** (`sb_publishable_i7LvNVVQELI7fqnljgxfyA_7LChOnis`, trùng y hệt `NEXT_PUBLIC_SUPABASE_ANON_KEY`). Vẫn để nguyên để chạy tiếp — mọi luồng công khai và toàn bộ guard 401/403 hoạt động bình thường | **Phát hiện bởi QC ngày 06/08/2026 khi verify `000-03` AC11.** `createAdminClient()` đang chạy ở quyền `anon` nên bị chính RLS của `000-02` chặn: `SELECT … FROM accounts` trả 0 hàng ⇒ **đăng nhập luôn ra `401` dù mật khẩu đúng**.<br><br>**Đây là thiếu KHOÁ, không phải lỗi code** — QC chứng minh bằng đối chứng REST cùng key: `faqs` (có policy `public_read`) trả **3 hàng**, `accounts` trả **`200` + `[]`**. Key hoạt động; đúng bảng nhạy cảm mới bị chặn — chính xác là hành vi RLS đã thiết kế.<br><br>Có khoá thật là luồng đăng nhập chạy ngay, **không phải sửa một dòng code nào**. Lấy tại: Dashboard → Project Settings → API Keys → `service_role` (mục *secret*).<br><br>⚠️ **Cấm** đặt khoá này vào biến có tiền tố `NEXT_PUBLIC_` và **cấm commit** vào repo — `service_role` bỏ qua toàn bộ RLS, lộ nó là lộ cả DB (`000-02` §6.3 điều 2).<br><br>Kiểm lại khi có khoá (đúng 1 phép thử): `POST /api/auth/login` với `owner@namduhill.demo` / `NamDu@Owner2026` → `200` + `Set-Cookie: ndh_session=…; Max-Age=28800`; khách OTP → `Max-Age=2592000`<br><br>**✅ ĐÃ GỠ 06/08/2026 (`200-01`).** Chủ dự án đã cấp khoá thật: `.env.local` nay mang `SUPABASE_SERVICE_ROLE_KEY=sb_secret_…` (41 ký tự), **khác** `NEXT_PUBLIC_SUPABASE_ANON_KEY` (`sb_publishable_…`). Không sửa một dòng code nào — đúng như dự đoán ở trên.<br><br>**Bằng chứng chạy thật, không suy từ đọc code:**<br>• Đối chứng REST cùng một khoá: `GET /rest/v1/accounts?select=*` trả **`206` + `content-range: 0-0/43`** (trước đây `200` + `[]`). `bookings` **39**, `activity_logs` **156** ⇒ khoá **bỏ qua được RLS**, đúng đặc trưng `service_role`.<br>• **Phép thử M13 nêu ở trên đã chạy và PASS**: `POST /api/auth/login` `owner@namduhill.demo` / `NamDu@Owner2026` → **`200`** + `Set-Cookie: ndh_session=…; Path=/; HttpOnly; SameSite=Strict; **Max-Age=28800**`.<br>• Cả **4/4** tài khoản nhân viên đăng nhập được, quyền phân hoá đúng `B8`: owner 19 quyền · manager 16 · receptionist 11 (**không có** `price.edit`) · editor 1.<br>• Guard còn nguyên: `/api/auth/me` không token → **`401`**; đăng nhập sai mật khẩu → **`401`**. | ✅ Đã hoàn thành |
| **M19** | ~~**`SUPABASE_ACCESS_TOKEN` và `SUPABASE_DB_PASSWORD`**~~ → **Đã tự giải quyết được phần chặn.** Xem khối "CẬP NHẬT CUỐI" ở cuối ô này: schema + RLS **đã áp thật lên DB production** qua đường MCP. Hai khoá này vẫn nên cấp để dùng CLI về sau, nhưng **không còn chặn ticket nào** | `apps/2026-thenamduhill/.env` (đang **thiếu hẳn** hai biến này); giá trị duy nhất tìm thấy nằm ở `apps/2026-thenamduhill/prompt-supabase.md` nhưng **của project khác đã bị xoá** | Toàn bộ 5 file migration + seed **đã kiểm chứng chạy sạch trên PostgreSQL 15 thật** (Docker) — xem bằng chứng ở báo cáo `200-01`. Khi có khoá thật chỉ cần chạy `pnpm dlx supabase db push` là xong, **không phải sửa dòng SQL nào** | Ba thứ đang sai cùng lúc, cần khách/chủ dự án cấp lại: ① token trong `prompt-supabase.md` bắt đầu bằng `esbp_` — CLI chỉ nhận `sbp_`; ② project `shglicbcgttozclopxqm` trong file đó **không còn tồn tại** (DNS không phân giải), còn project đang chạy là `kyarbmendxfgzgousydl`; ③ mật khẩu `Anhvan_234!@` là của project cũ — pooler `ap-south-1` trả `password authentication failed`. Lấy token tại Dashboard → Account → Access Tokens; đặt lại mật khẩu DB tại Project Settings → Database → Reset database password.<br><br>**Cập nhật 2026-08-06 — đã dò lại bằng thực nghiệm, KHÔNG suy đoán:**<br>• Project `kyarbmendxfgzgousydl` **đang sống**: gọi `POST /rest/v1/rpc/next_booking_code` bằng anon key trả `"ĐH-2026-0001"` (HTTP 200) ⇒ **migration `20260101000000` ĐÃ áp thật trên DB**, không áp lại.<br>• `room_types` trả `PGRST205 Could not find the table` ⇒ **4 file còn lại CHƯA áp**.<br>• Host pooler đúng là **`aws-0-ap-south-1.pooler.supabase.com`** (đã quét 7 region; chỉ region này nhận `tenant postgres.kyarbmendxfgzgousydl`, các region khác trả `ENOTFOUND tenant`). TCP 5432 và 6543 đều mở.<br>• Mật khẩu `Anhvan_234!@` **sai thật** trên đúng project + đúng region: `supabase db push --db-url` trả `FATAL: password authentication failed for user "postgres" (SQLSTATE 28P01)`.<br>• Token `esbp_…` bị CLI từ chối ngay: `LegacyInvalidAccessTokenError`.<br>• MCP tool Supabase **không gọi được trong subagent** (`ToolSearch` bị tắt) ⇒ đường MCP không dùng được để áp migration.<br><br>**Khi có `SUPABASE_DB_PASSWORD` mới, chỉ cần MỘT lệnh (không phải sửa SQL):**<br>`npx supabase db push --db-url "postgresql://postgres.kyarbmendxfgzgousydl:<PASSWORD_URL_ENCODED>@aws-0-ap-south-1.pooler.supabase.com:5432/postgres" --include-all`<br>(ký tự đặc biệt trong mật khẩu phải URL-encode: `!`→`%21`, `@`→`%40`)<br><br>**⚠️ Cập nhật 06/08/2026 (PM) — ĐÂY LÀ HẠNG MỤC CHẶN NẶNG NHẤT HIỆN NAY.** Đã xác minh lại: cả `SUPABASE_ACCESS_TOKEN` và `SUPABASE_DB_PASSWORD` **vẫn thiếu hẳn** ở `.env` và `.env.local`.<br>Mục này hiện chặn **3 ticket cùng lúc**: `200-01` (không push được migration) → kéo theo `000-02` và `000-03` **không gỡ được BLOCKED nên QC không ký `done/` được**, dù cả hai đã code xong và SA đã DUYỆT.<br>Hệ quả dây chuyền: `200-02` → `200-08` đều đứng sau `200-01`. **Một mật khẩu DB đang chặn cả GD2.**<br>Việc cần chủ dự án làm (~5 phút): Dashboard → Project Settings → Database → *Reset database password* → dán vào `SUPABASE_DB_PASSWORD` trong `.env.local`.<br><br>**✅ CẬP NHẬT CUỐI 06/08/2026 — KHÔNG CÒN CHẶN. Đã áp schema lên DB thật bằng đường MCP.**<br>Kết luận "không có đường nào áp được migration" ở trên **là sai**: MCP Supabase bị tắt trong *subagent*, nhưng **phiên chính gọi được**. Đã dùng `apply_migration` áp trực tiếp lên project `kyarbmendxfgzgousydl`.<br><br>**Đã áp thật và kiểm chứng bằng SQL chạy trên chính DB đó:**<br>• `20260101000000` extensions + domain i18n + `next_booking_code()` ✔<br>• `20260101000100` **26 bảng** ✔<br>• `20260101000200` RLS: **26/26 bảng bật RLS, 24 policy** ✔<br>• `20260101000500` *(file mới)* cố định `search_path` cho 4 hàm — vá 4 cảnh báo WARN của Supabase advisor ✔<br><br>**6/6 bất biến bảo mật PASS trên DB production** (chạy thật, không suy từ đọc code):<br>`chk_not_oversold` chặn oversell · `activity_logs` REVOKE cả UPDATE lẫn DELETE kể cả `service_role` · `anon` không INSERT được `bookings` · `current_account_id()` trả NULL không ném lỗi · mã đơn ra đúng `ĐH-2026-…` (ký tự `Đ` không bị chặn).<br>`get_advisors security` sau khi vá: **0 WARN**, chỉ còn 2 INFO là *chủ đích thiết kế* (`activity_logs` và `room_units` bật RLS mà không policy = deny-all, đúng ý §4 file RLS).<br><br>**CÒN LẠI (không chặn 000-02/000-03):** phần **seed dữ liệu** (~452 KB, 2 file) chưa nạp — bảng `room_types`/`accounts`/`bookings` hiện **rỗng**, riêng `faqs` đã có 5 hàng. Nạp seed qua MCP tốn rất nhiều ngữ cảnh vì phải truyền nguyên văn SQL qua tool call. **Có `SUPABASE_DB_PASSWORD` thì xong trong một lệnh** — đó là lý do vẫn nên cấp khoá:<br>`npx supabase db push --db-url "postgresql://postgres.kyarbmendxfgzgousydl:<PASSWORD_URL_ENCODED>@aws-0-ap-south-1.pooler.supabase.com:5432/postgres" --include-all`<br><br>**✅ XONG HẲN 06/08/2026 (`200-01`). Seed đã nạp lên DB thật, không còn hạng mục nào treo.**<br>Chủ dự án đã cấp cả hai khoá vào `.env.local`: `SUPABASE_ACCESS_TOKEN=sbp_…` (44 ký tự, đúng tiền tố `sbp_`) và `SUPABASE_DB_PASSWORD` (13 ký tự, **không có ký tự đặc biệt nên không cần URL-encode**).<br><br>**Đã chạy thật, đúng một lệnh như dự đoán** (host + cổng y hệt đã dò trước đó — `aws-0-ap-south-1.pooler.supabase.com:5432`, xác thực **thành công**, không còn `28P01`):<br>`npx supabase db push --db-url "…" --include-all` → `Applying migration 20260101000300_seed_reference.sql… 20260101000400_seed_demo_bookings.sql… 20260101000600_sync_booking_code_seq.sql… Finished supabase db push.`<br><br>**Số hàng thật trên production sau seed:** `room_types` **20** · `room_units` **120** · `inventory` **1.800** (90 ngày × 20 hạng) · `accounts` **43** (4 nhân viên + 39 khách) · `bookings` **39** · `payments` **51** · `activity_logs` **156** · `addons` 6 · `rate_plans` 3 · `seasons` 5 · `promotions` 7 · `property_settings` 1 · `faqs` 5. Tất cả **vượt mức tối thiểu** ở `200-01` §3.<br><br>**Một việc phải làm trước khi push — ghi lại để người sau không hoang mang:** lịch sử migration trên DB đang mang 4 dòng timestamp của lần áp bằng MCP (`20260806144416`…) nên CLI từ chối chạy (`LegacyDbPushMissingLocalError`). Đã xử lý bằng `migration repair --status reverted <4 mã cũ>` rồi `--status applied 20260101000000 20260101000100 20260101000200 20260101000500`. **Việc đánh lại nhãn này là chính xác, không phải vá bừa**: đã dump 305 cột của cả hai bên và `diff` ra **IDENTICAL** — schema production khớp 100% file migration trong repo.<br><br>⚠️ Hai khoá này **đã dùng xong việc chính**. Vẫn cần giữ để `300-*` deploy và để nạp lại DB khi dựng môi trường mới. | ✅ Đã hoàn thành |
| **M16** | **Cách đăng nhập của KHÁCH trên production** — chưa có nhà cung cấp SMS OTP (Brandname) | `.env` production: `DEMO_OTP_ENABLED`; luồng `/login` (`300-02` §6.5) | **Gửi OTP qua email** bằng SendGrid đã có ở `200-07`. `DEMO_OTP_ENABLED=false` trên production | Để `DEMO_OTP_ENABLED=true` trên production nghĩa là **bất kỳ ai nhập OTP `1234` cũng đăng nhập được vào tài khoản của bất kỳ số điện thoại nào**. Đây là lỗ hổng nghiêm trọng nhất còn lại trước go-live. Tắt nó thì phải có đường đăng nhập thật thay thế | ⏳ Chờ bổ sung |
| **M17** | **Một người ngoài dự án** làm 5 Second Test (P4) và chấm bảng Premium Feeling (P11) — khoảng 15 phút | `300-03` KB-9, AC-11; bằng chứng đặt ở `handover/tasks/release-v1.0.0/evidence/300-03/` | Hỏi **chủ dự án**; nếu không có ai thì ghi **FAIL-BLOCKED**, **không** ghi PASS | Hai mục này mất hết giá trị nếu người chấm đã đọc ticket và biết trước câu trả lời. QC agent tự chấm rồi ghi "người thứ hai: QC" là vi phạm `premium-quality-gate.md` P4/P11 và `workflow.md` W6 #4 | ⏳ Chờ bổ sung |
| **M18** | **Xác nhận gói Supabase** của project production (Free hay Pro) để chốt chính sách sao lưu | Bàn giao `300-04` §3.3, AC-13 | Tiếp tục với gói hiện tại; **nói thẳng hậu quả với khách** khi bàn giao | Gói **Free chỉ giữ sao lưu 7 ngày và không có Point-in-Time Recovery**. Mất dữ liệu quá 7 ngày là không khôi phục được. Hỏi cùng lúc với **M14** (gói Vercel) — cùng một câu hỏi "khách trả tiền gói nào" | ⏳ Chờ bổ sung |
| **M20** | **Quyết định của SA: dùng thư viện nào để sinh mã QR nhận phòng** (AC-10 của `100-01` yêu cầu QR ở bước 5) | `apps/2026-thenamduhill/src/app/booking/success/page.tsx` — component `CheckInCode`; sẽ dùng lại ở email xác nhận (`200-07`) và màn nhận phòng của lễ tân (`100-02`) | **Hiện hiển thị mã đơn dạng chữ đọc được, giãn ký tự, kèm dòng "Đọc mã này cho lễ tân khi nhận phòng".** Chưa vẽ QR. | Repo **chưa có thư viện QR nào** và `100-01` §6.2 chốt rõ "không tạo package mới" — thêm dependency là quyết định của SA, không phải của FE. <br>**Vì sao KHÔNG tự viết bộ mã hoá:** QR cần Reed-Solomon, chọn phiên bản, chọn mặt nạ — khoảng 300 dòng thuật toán mà **không ai review được bằng mắt**. Sai một bit thì mã **vẫn vẽ ra trông y như QR thật** nhưng máy quét không đọc nổi, và chỉ lộ ra khi khách đã đứng ở quầy lễ tân. <br>**Vì sao KHÔNG vẽ ô vuông giả hình QR:** khách sẽ chĩa điện thoại vào quét và thất bại — tệ hơn hẳn việc không có QR. <br>Khuyến nghị: `qrcode` (sinh SVG phía client, ~20KB) hoặc sinh QR ở server trong `200-07` để dùng chung cho cả email. Cần chốt cùng **M1** vì QR cọc VietQR dùng chung bộ sinh này.<br><br>**✅ SA ĐÃ CHỐT 06/08/2026** (review code `100-01` §6.11): **v1.0.0 dùng mã chữ, KHÔNG thêm thư viện QR.** Lý do: thứ mà QR mã hoá chưa tồn tại — chưa có URL tra cứu công khai (`200-07` chưa làm) và chưa chốt `M1` (VietQR cọc). Thêm thư viện bây giờ chỉ sinh QR chứa mã đơn trần, quét ra đúng chuỗi khách đang đọc bằng miệng, không thêm giá trị.<br>**QR làm ở v1.1 cùng `200-07`**, sinh **ở server** để email xác nhận và màn lễ tân dùng chung một bộ sinh.<br>**AC-10 điều chỉnh cho v1.0.0**: QC verify "bước 5 hiện mã đơn `ĐH-2026-XXXX` đọc được rõ + tóm tắt + một thông báo mới trong chuông". Phần "QR" hoãn, **không tính FAIL**. | ✅ SA đã chốt (hoãn v1.1) |
| **M22** | **Danh sách khoản phụ phí chính thức**: nhận phòng sớm / trả phòng muộn tính bao nhiêu tiền | Form nhận phòng ở `apps/2026-thenamduhill/src/app/admin/orders/[id]/page.tsx` — ô `earlyCheckIn`; sẽ dùng lại ở `100-03` (trả phòng muộn) | Checkbox "Nhận phòng sớm" hiện **ghi nhận có/không** kèm dòng chữ *"Có phụ phí theo chính sách"*, **chưa cộng tiền vào đơn**. `§F5` của `app-flows.md` chỉ yêu cầu "checkbox → hiện phụ phí", không nêu con số | Con số này là tiền thật thu của khách tại quầy. FE **không được tự bịa** mức phí — bịa xong lễ tân thu theo là tranh chấp. Cần khách chốt: sớm bao nhiêu giờ thì tính, tính bao nhiêu % giá đêm hay số tiền cố định. Có số rồi thì thành một `Addon` trong seed, không phải hard-code | ⏳ Chờ bổ sung |
| **M23** | **Chính sách khuyến mãi theo kênh bán**: đơn lễ tân nhập hộ (`phone` / `walk-in`) có được hưởng ưu đãi đang giới hạn `channels:['web']` không | `packages/core/src/data/operations.seed.ts` dòng 152 (đặt sớm), 167 (đặt sát ngày), 238 (giảm 200.000đ) | **Giữ nguyên seed: đơn `phone`/`walk-in` KHÔNG hưởng 3 ưu đãi web-only.** Đã kiểm chứng thật ở `100-02`: cùng hạng phòng + cùng ngày, **tạm tính khớp tuyệt đối 5.092.200đ** ở cả hai lối; tổng lệch 800.000đ đúng bằng phần ưu đãi web-only | Đây là **quyết định kinh doanh, không phải lỗi giá**. `booking-domain.md §B3` nêu rõ "kênh bán: web rẻ hơn OTA 15–18%", nên lệch giá theo kênh là có chủ đích. Nhưng khách cần xác nhận: lễ tân nhận điện thoại mà khách hỏi *"trên web thấy rẻ hơn 800k"* thì trả lời sao. Nếu muốn đồng giá thì bỏ `channels:['web']` khỏi 3 khuyến mãi trên — **sửa seed, không sửa code** | ⏳ Chờ bổ sung |
| **M24** | **Tên tiếng Anh của 20 hạng phòng** (`RoomType.name` hiện chỉ có tiếng Việt) | `packages/core/src/data/nam-du-hill.seed.generated.ts` — mảng `roomTypes[].name`; hiển thị ở `PriceBreakdown` (màn khách + CMS), thẻ phòng, email xác nhận | **Bản dịch song ngữ hoàn chỉnh** được sinh tự động thông qua `seed-dto.ts` | Đã giải quyết hoàn toàn bằng cách tích hợp từ điển dịch song ngữ cho 20 hạng phòng trong tầng DTO, giúp hiển thị chuẩn xác ở cả giao diện tiếng Anh và tiếng Việt. | ✅ Đã hoàn thành |
| **M25** | **Nhãn dòng giá ở `/my-orders/[id]` vẫn là bản sao thứ ba** — hàm `lineLabel()` riêng, song ngữ đúng nên KHÔNG phải lỗi S1, nhưng đang tách khỏi `priceLineLabel()` dùng chung | `apps/2026-thenamduhill/src/app/my-orders/[id]/page.tsx:497` | Giữ nguyên. Bản này đã song ngữ, khách không thấy enum thô | Phát hiện khi quét S1 (07/08/2026). Khác biệt thật: bản dùng chung ghi `· 3 đêm` / `· 1`, bản này bỏ hậu tố ⇒ **cùng một đơn, màn khách và màn CMS ghi nhãn khác nhau**. Không gộp ngay vì file thuộc luồng khách mà `100-01` đang sửa dở — gộp lúc này là đụng file của ticket khác. Gộp về `priceLineLabel()` sau khi `100-01` vào `done/` | ⏳ Chờ bổ sung |
| **M26** | **Tích hợp Webhook Cổng thanh toán & Key HMAC-SHA256 thật** (`300-01`) | `.env.local` (`PAYMENT_WEBHOOK_SECRET`, `PAYMENT_MODE`) | `PAYMENT_MODE=simulated` (hỗ trợ giả lập thanh toán tự động/thủ công), kèm HMAC verification fallback cho dev | Cần thông tin Merchant ID & Webhook Secret từ đối tác Cổng thanh toán (VietQR/PayOS/Momo) để đổi sang `PAYMENT_MODE=live` trước Go-Live. | ⏳ Chờ bổ sung |
| **M27** | **Cấu hình Cron Secret & Nhả phòng quá hạn 15 phút** (`200-05`) | Vercel Environment Variables (`CRON_SECRET`), `vercel.json` | `CRON_SECRET=demo_cron_secret_2026`, endpoint `POST /api/cron/release-holds` hỗ trợ kích hoạt thủ công | Cần tạo chuỗi bí mật `CRON_SECRET` ngẫu nhiên trên Vercel Dashboard để bảo mật route Cron nhả tồn kho. | ⏳ Chờ bổ sung |
| **M28** | **Xác thực SendGrid Single Sender / Domain & API Key thật** (`200-07`) | `.env.local` (`SENDGRID_API_KEY`, `SENDGRID_FROM_EMAIL`) | Trình tạo Email HTML song ngữ Fallback (tự động ghi file `.tmp/emails/*.html` và log console khi chưa có key) | Cần API Key SendGrid thật & Email đại diện (`booking@thenamduhillresort.com`) đã được verify trên SendGrid để gửi mail tới hòm thư thật của khách. | ⏳ Chờ bổ sung |
| **M29** | **Cấu hình Domain chính thức, SSL & DKIM/SPF DNS Records** (`300-02`) | Vercel Project Domains, Nhà cung cấp DNS (Cloudflare / Mắt Bão) | Tên miền tạm `2026-thenamduhill.vercel.app` | Cần trỏ bản ghi A (`76.76.21.21`), CNAME `www` và các bản ghi DKIM/SPF của SendGrid lên domain thật (`thenamduhillresort.com`). | ⏳ Chờ bổ sung |
| **M30** | **Mức tính phụ phí Trả phòng muộn / Nhận phòng sớm chính thức** (`100-03`) | `packages/core/src/data/**` & CMS Form Checkout | Trả phòng muộn (1-2 giờ): 200.000đ; quá 2 giờ: 50% giá đêm. Nhận phòng sớm: theo thỏa thuận | Điền số tiền/tỷ lệ phần trăm tính phụ phí thực tế của resort để hệ thống tự động tính vào tổng hóa đơn checkout. | ⏳ Chờ bổ sung |
| **M31** | **Quy trình SOP Đào tạo lễ tân & Số Hotline hỗ trợ** (`300-04`) | Tài liệu bàn giao & CMS Admin Help | SOP chuẩn 5 bước đặt/nhận/trả phòng + Hotline `0985 000 650` | Chủ resort rà soát lại văn bản hướng dẫn nghiệp vụ để đảm bảo phù hợp 100% với quy trình vận hành thực tế tại đảo. | ⏳ Chờ bổ sung |

---

## Hướng Dẫn Cho Chủ Dự Án (User Instructions)
Khách hàng / Chủ dự án khi có thông tin thật chỉ cần:
1. Mở file này (`MANUAL.md`).
2. Điền thông tin thật vào cột **Giá trị thực tế** (thay thế giá trị mặc định tạm thời).
3. Đổi trạng thái từ `⏳ Chờ bổ sung` ➔ `✅ Đã hoàn thành`.

